// # Server config file!

var config = {
    // Paths to be used by server 
    'paths': {
        'view': __dirname + '/view',
        'public': __dirname + '/public'
    },
    
    // Server Options
    'server': {
        'port': '4010'
    }
};

module.exports = config;